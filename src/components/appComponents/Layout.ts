import { Aside } from "./Aside";
import { Main } from "./Main";
import * as BUI from "@thatopen/ui";
export const Layout = BUI.Component.create(() => {
    return BUI.html `
      <div id="mainApp" class="flex flex-1 bg-gray-100">
        
      </div>
      `
  })
